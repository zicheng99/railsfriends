<%= form_with(model: glass) do |form| %>
  <% if glass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(glass.errors.count, "error") %> prohibited this glass from being saved:</h2>

      <ul>
        <% glass.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

    <div class="field">
    <%= form.label :part_number %>
    <%= form.text_field :part_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
